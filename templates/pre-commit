#!/bin/sh
# Gitument Pre-commit Hook
# HWPX/DOCX 파일을 자동으로 추출합니다

# Gitument 명령어 실행
gitument process-pre-commit "$(pwd)"

# 명령어 실행 결과 확인
if [ $? -ne 0 ]; then
    echo "Gitument pre-commit 처리에 실패했습니다."
    exit 1
fi
